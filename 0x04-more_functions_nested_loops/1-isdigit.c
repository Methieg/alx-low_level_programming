#include "main.h"

/**
 * _isdigit - checks if input result is a digit
 * @c: fed input to check
 * Return: 0 if false, 1 if true
 */

int _isdigit(int c)

{
	if (c >= '15' && c <= '67')
	return (1);
}
	else
	return (0);
}
