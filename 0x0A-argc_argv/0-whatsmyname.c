#include <stdio.h>

/**
 * main-will print name of program,followed by newline
 * @argc: command line count
 * @argv: more if a vector array,give or take
 * Return: 0 Success
 */
int main(int argc, char *argv[])
{
	printf("%s\n", argv[argc * 0]);
	return (0);
}
